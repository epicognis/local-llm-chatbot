# llm-hw-monitor — standalone deps (kept deliberately minimal).
#   psutil   : cross-platform CPU / memory / per-process sampling
#   requests : Ollama /api/ps HTTP introspection
#   rich     : the live terminal dashboard
# GPU power/temp/VRAM come from the `nvidia-smi` binary (NVIDIA driver), not pip.
psutil>=5.9
requests>=2.28
rich>=13.0
